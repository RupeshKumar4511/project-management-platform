version: 0.2

env:
  parameter-store:
    DOCKER_REGISTRY_USERNAME: /myapp/docker-credentials/username
    DOCKER_REGISTRY_PASSWORD: /myapp/docker-credentials/password
    VITE_API_URL: /myapp/frontend/api-url
    VITE_BACKEND_URL: /myapp/frontend/backend-url
    VITE_FRONTEND_URL: /myapp/frontend/frontend-url
    VITE_RAZORPAY_API_KEY_ID: /myapp/frontend/razorpay-key-id

phases:
  install:
    runtime-versions:
      nodejs: 24

  pre_build:
    commands:
      - set -e
      - echo "Logging into Docker Hub..."
      - echo $DOCKER_REGISTRY_PASSWORD | docker login -u $DOCKER_REGISTRY_USERNAME --password-stdin

  build:
    commands:
      - set -e
      - echo "Building frontend image..."
      - |
        docker build \
          --build-arg VITE_API_URL=$VITE_API_URL \
          --build-arg VITE_BACKEND_URL=$VITE_BACKEND_URL \
          --build-arg VITE_FRONTEND_URL=$VITE_FRONTEND_URL \
          --build-arg VITE_RAZORPAY_API_KEY_ID=$VITE_RAZORPAY_API_KEY_ID \
          -t $DOCKER_REGISTRY_USERNAME/projectly-frontend:latest \
          -f frontend/Dockerfile ./frontend

      - docker tag $DOCKER_REGISTRY_USERNAME/projectly-frontend:latest $DOCKER_REGISTRY_USERNAME/projectly-frontend:$CODEBUILD_RESOLVED_SOURCE_VERSION

  post_build:
    commands:
      - set -e
      - echo "Pushing frontend image..."

      - docker push $DOCKER_REGISTRY_USERNAME/projectly-frontend:latest
      - docker push $DOCKER_REGISTRY_USERNAME/projectly-frontend:$CODEBUILD_RESOLVED_SOURCE_VERSION